
{{> _header}}

<main class="min-h-screen bg-gradient-to-br from-background via-background to-gray-50">
  <div class="container mx-auto px-4 py-8 text-center">
    
    <div class="max-w-2xl mx-auto">
      
      <!-- Error Icon and Code -->
      <div class="mb-8">
        {{#if error.status_code}}
        {{#compare error.status_code '==' 404}}
        <div class="text-8xl mb-6">🔍</div>
        <h1 class="text-6xl font-bold text-gray-800 mb-4">404</h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">Página no encontrada</h2>
        <p class="text-gray-500 mb-8">
          Lo sentimos, la página que buscas no existe o ha sido movida.
        </p>
        {{else}}
        {{#compare error.status_code '==' 403}}
        <div class="text-8xl mb-6">🚫</div>
        <h1 class="text-6xl font-bold text-gray-800 mb-4">403</h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">Acceso denegado</h2>
        <p class="text-gray-500 mb-8">
          No tienes permisos para acceder a esta página.
        </p>
        {{else}}
        {{#compare error.status_code '==' 500}}
        <div class="text-8xl mb-6">⚠️</div>
        <h1 class="text-6xl font-bold text-gray-800 mb-4">500</h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">Error del servidor</h2>
        <p class="text-gray-500 mb-8">
          Ha ocurrido un error interno. Por favor, inténtalo más tarde.
        </p>
        {{else}}
        <div class="text-8xl mb-6">❌</div>
        <h1 class="text-6xl font-bold text-gray-800 mb-4">{{error.status_code}}</h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">Error</h2>
        <p class="text-gray-500 mb-8">
          {{#if error.message}}{{error.message}}{{else}}Ha ocurrido un error inesperado.{{/if}}
        </p>
        {{/compare}}
        {{/compare}}
        {{/compare}}
        {{else}}
        <div class="text-8xl mb-6">❌</div>
        <h1 class="text-6xl font-bold text-gray-800 mb-4">Error</h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">Ha ocurrido un error</h2>
        <p class="text-gray-500 mb-8">
          Ha ocurrido un error inesperado.
        </p>
        {{/if}}
      </div>

      <!-- Actions -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
        <a href="{{help_center.url}}" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors">
          Ir al inicio
        </a>
        <button onclick="history.back()" class="border border-gray-300 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-50 transition-colors">
          Volver atrás
        </button>
      </div>

      <!-- Help Section -->
      <div class="bg-blue-50 rounded-lg p-6">
        <h3 class="text-lg font-semibold mb-4">¿Necesitas ayuda?</h3>
        <p class="text-gray-600 mb-4">
          Si continúas teniendo problemas, no dudes en contactarnos.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="tel:6001234567" class="text-blue-600 hover:text-blue-800">
            📞 600 123 4567
          </a>
          <a href="https://wa.me/56987654321" class="text-green-600 hover:text-green-800" target="_blank">
            💬 WhatsApp
          </a>
          <span class="text-gray-600">
            ✉️ info@adtchile.cl
          </span>
        </div>
      </div>

    </div>
    
  </div>
</main>

{{> _footer}}
