
{{> header}}

<main class="min-h-screen bg-gradient-to-br from-background via-background to-gray-50">
  <div class="container mx-auto px-4 py-8 max-w-6xl">
    
    <!-- Search Header -->
    <div class="mb-8">
      <h1 class="text-4xl font-bold mb-4">Resultados de búsqueda</h1>
      {{#if query}}
      <p class="text-lg text-gray-600">
        Resultados para: <strong>"{{query}}"</strong>
      </p>
      {{/if}}
    </div>

    <!-- Search Results -->
    {{#if results}}
    <div class="space-y-6">
      {{#each results}}
      <article class="bg-white rounded-lg shadow-md p-6">
        <div class="flex items-start justify-between mb-3">
          <div class="flex items-center gap-2 text-sm text-gray-500">
            <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded-full text-xs">
              Artículo
            </span>
            {{#if promoted}}
            <span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded-full text-xs">Destacado</span>
            {{/if}}
          </div>
          
          <span class="text-sm text-gray-500">{{vote_sum}} 👍</span>
        </div>
        
        <h2 class="text-xl font-semibold mb-3">
          <a href="{{url}}" class="hover:text-blue-600 transition-colors">{{title}}</a>
        </h2>
        
        <p class="text-gray-600 mb-4 line-clamp-3">{{excerpt}}</p>
        
        <div class="flex items-center justify-between text-sm text-gray-500">
          <div class="flex items-center gap-4">
            <span>Actualizado hace tiempo</span>
          </div>
        </div>
      </article>
      {{/each}}
    </div>
    {{else}}
    <!-- No Results -->
    <div class="text-center py-12">
      <div class="text-6xl mb-4">🔍</div>
      <h2 class="text-2xl font-bold mb-4">No se encontraron resultados</h2>
      <p class="text-gray-600 mb-8">
        {{#if query}}
        No encontramos artículos que coincidan con "{{query}}".
        {{else}}
        Introduce un término de búsqueda para encontrar artículos.
        {{/if}}
      </p>
      
      <div class="max-w-md mx-auto">
        <form action="/hc/es/search" method="get" class="relative">
          <input 
            type="search" 
            name="query" 
            placeholder="Buscar artículos..." 
            value="{{query}}"
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
          <button 
            type="submit" 
            class="absolute right-1 top-1 bg-blue-600 text-white p-2 rounded-lg hover:bg-blue-700 transition-colors"
          >
            🔍
          </button>
        </form>
      </div>
    </div>
    {{/if}}

  </div>
</main>

{{> footer}}
