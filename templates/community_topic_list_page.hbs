{{> document_head}}
{{> header}}

<main class="min-h-screen bg-gradient-to-br from-background via-background to-gray-50">
  <div class="container mx-auto px-4 py-8 max-w-6xl">
    
    <!-- Breadcrumb -->
    <nav class="mb-6">
      <ol class="flex items-center space-x-2 text-sm text-gray-500">
        <li><a href="{{help_center.url}}" class="hover:text-blue-600">Inicio</a></li>
        <li>></li>
        <li class="text-gray-800 font-medium">Comunidad</li>
      </ol>
    </nav>

    <!-- Community Header -->
    <div class="mb-8">
      <h1 class="text-4xl font-bold mb-4">Comunidad ADT Chile</h1>
      <p class="text-lg text-gray-600">
        Conecta con otros usuarios, comparte experiencias y encuentra respuestas en nuestra comunidad.
      </p>
    </div>

    <!-- Topics List -->
    {{#if topics}}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {{#each topics}}
      <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow p-6">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 bg-blue-600 text-white rounded-lg flex items-center justify-center">
            💬
          </div>
          <h2 class="text-xl font-semibold">
            <a href="{{url}}" class="hover:text-blue-600 transition-colors">{{name}}</a>
          </h2>
        </div>
        
        {{#if description}}
        <p class="text-gray-600 mb-4">{{description}}</p>
        {{/if}}
        
        <div class="flex items-center justify-between text-sm text-gray-500">
          <span>0 publicaciones</span>
          <span>0 seguidores</span>
        </div>
      </div>
      {{/each}}
    </div>
    {{else}}
    <!-- Empty State -->
    <div class="text-center py-12">
      <div class="text-6xl mb-4">💬</div>
      <h2 class="text-2xl font-bold mb-4">No hay temas disponibles</h2>
      <p class="text-gray-600">
        Los temas de la comunidad aparecerán aquí una vez que se configuren.
      </p>
    </div>
    {{/if}}

    <!-- Community Guidelines -->
    <div class="bg-blue-50 rounded-lg p-6 mt-8">
      <h3 class="font-semibold text-blue-900 mb-3">Bienvenido a la Comunidad ADT</h3>
      <p class="text-sm text-blue-800 mb-4">
        Nuestra comunidad es un espacio para que los usuarios de ADT Chile se ayuden mutuamente y compartan experiencias.
      </p>
      <ul class="text-sm text-blue-800 space-y-1">
        <li>• Mantén un tono respetuoso y profesional</li>
        <li>• Comparte tus experiencias y conocimientos</li>
        <li>• Busca antes de publicar para evitar duplicados</li>
        <li>• Ayuda a otros usuarios con tus respuestas</li>
      </ul>
    </div>

  </div>
</main>

{{> footer}}
