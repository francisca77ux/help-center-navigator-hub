
{{> _header}}

<main class="min-h-screen bg-gradient-to-br from-background via-background to-gray-50">
  <!-- Hero Section -->
  <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-20">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-5xl font-bold mb-6">¿En qué podemos ayudarte?</h1>
      <p class="text-xl mb-8 text-blue-100">
        Encuentra respuestas rápidas a tus preguntas sobre seguridad ADT
      </p>
      
      <div class="max-w-2xl mx-auto">
        <form action="/hc/es/search" method="get" class="relative">
          <input 
            type="search" 
            name="query" 
            placeholder="Buscar artículos de ayuda..." 
            class="w-full px-6 py-4 text-lg rounded-full text-gray-800 focus:outline-none focus:ring-4 focus:ring-blue-300"
          >
          <button 
            type="submit" 
            class="absolute right-2 top-2 bg-blue-600 text-white p-3 rounded-full hover:bg-blue-700 transition-colors"
          >
            🔍
          </button>
        </form>
      </div>
    </div>
  </section>

  <!-- Categories Section -->
  <section class="py-16">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Categorías de Ayuda</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {{#each categories}}
        <a href="{{url}}" class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow p-6 group">
          <div class="flex items-center mb-4">
            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
              {{#contains name "Panel"}}🛡️{{/contains}}
              {{#contains name "App"}}📱{{/contains}}
              {{#contains name "Servicios"}}⚙️{{/contains}}
              {{#contains name "Facturación"}}💳{{/contains}}
              {{#contains name "Instalación"}}🔧{{/contains}}
              {{#contains name "Cámaras"}}📹{{/contains}}
              {{#contains name "Monitoreo"}}👁️{{/contains}}
              {{#contains name "Soporte"}}🛠️{{/contains}}
            </div>
            <h3 class="text-xl font-semibold group-hover:text-blue-600 transition-colors">{{name}}</h3>
          </div>
          <p class="text-gray-600 mb-4">{{description}}</p>
          <div class="text-sm text-blue-600 font-medium">
            Ver artículos
          </div>
        </a>
        {{/each}}
      </div>
    </div>
  </section>

  <!-- Popular Articles -->
  <section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Artículos Populares</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {{#each promoted_articles}}
        <article class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow p-6">
          <div class="flex items-start justify-between mb-3">
            <span class="text-xs bg-blue-100 text-blue-800 px-2 py-1 rounded-full">
              {{#if section}}{{section.name}}{{else}}General{{/if}}
            </span>
            <span class="text-sm text-gray-500">{{vote_sum}} 👍</span>
          </div>
          
          <h3 class="text-lg font-semibold mb-3">
            <a href="{{url}}" class="hover:text-blue-600 transition-colors">{{title}}</a>
          </h3>
          
          <p class="text-gray-600 text-sm">{{excerpt}}</p>
        </article>
        {{/each}}
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-16">
    <div class="container mx-auto px-4">
      <div class="bg-gradient-to-r from-blue-50 to-blue-100 rounded-2xl p-8 text-center">
        <h2 class="text-2xl font-bold mb-4">¿Necesitas más ayuda?</h2>
        <p class="text-gray-600 mb-6">
          Nuestro equipo de atención al cliente está disponible para ayudarte
        </p>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="text-center">
            <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4">
              📞
            </div>
            <h4 class="font-semibold mb-2">Teléfono</h4>
            <a href="tel:6001234567" class="text-blue-600 hover:underline">600 123 4567</a>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-green-600 text-white rounded-full flex items-center justify-center mx-auto mb-4">
              💬
            </div>
            <h4 class="font-semibold mb-2">WhatsApp</h4>
            <a href="https://wa.me/56987654321" class="text-green-600 hover:underline" target="_blank">
              +56 9 8765 4321
            </a>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-purple-600 text-white rounded-full flex items-center justify-center mx-auto mb-4">
              ✉️
            </div>
            <h4 class="font-semibold mb-2">Email</h4>
            <span class="text-gray-600">info@adtchile.cl</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</main>

{{> _footer}}
